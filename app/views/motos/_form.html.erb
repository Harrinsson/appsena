<%= form_with(model: moto) do |form| %>
  <% if moto.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(moto.errors.count, "error") %> prohibited this moto from being saved:</h2>

      <ul>
        <% moto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id_propietario, style: "display: block" %>
    <%= form.number_field :id_propietario %>
  </div>

  <div>
    <%= form.label :marca, style: "display: block" %>
    <%= form.text_field :marca %>
  </div>

  <div>
    <%= form.label :matricula, style: "display: block" %>
    <%= form.text_field :matricula %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
